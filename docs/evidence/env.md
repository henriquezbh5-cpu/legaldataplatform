# Environment snapshot

> **Pending real capture.** Run `python scripts/capture_env.py > docs/evidence/env.md` after starting the stack to overwrite this placeholder with actual host data.

The capture script will record:

- OS and kernel version
- Python runtime version
- CPU cores and RAM
- Docker + Docker Compose versions
- Current git commit hash
- Working-tree cleanliness
- Running container list
